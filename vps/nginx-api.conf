server {
    listen 80;
    server_name api.qweercoin.com;
    # Redirect to HTTPS (managed by certbot)
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name api.qweercoin.com;

    # TLS — managed by certbot
    ssl_certificate     /etc/letsencrypt/live/api.qweercoin.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.qweercoin.com/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    # Security headers
    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options DENY;

    location / {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
